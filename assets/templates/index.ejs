<% include _partials/_head %>
  <body>
    <main>
      <!-- Main navigation, list items, circle links -->
      <% include _partials/_mainnav %>

      <!-- Cover section -->
      <% include _partials/_cover %>

      <!-- Intro section -->
      <% include _partials/_intro %>

      <!-- year in numbers part one, operations -->
      <% include _partials/_numbers %>

      <!-- year in numbers part two, IM products and social media -->
      <% include _partials/_numberssocial %>

      <!-- year in numbers part 3, staff, global meetings and trainings -->
      <% include _partials/_numberscont %>

      <!-- Foreword section -->
	  <% include _partials/_foreword %>
	  
	  <!-- Foreword section -->
      <% include _partials/_sag %>

      <!-- year operations section -->
      <% include _partials/_emergencies %>

      <!-- Bangladesh section -->
      <% include _partials/_bangladesh %>

      <!-- Cameroon section -->
      <% include _partials/_cameroon %>

      <!-- CAR section -->
      <% include _partials/_car %>

      <!-- DRC section -->
      <% include _partials/_drc %>

      <!-- Indonesia section -->
      <% include _partials/_indonesia %>

      <!-- Iraq section -->
      <% include _partials/_iraq %>

      <!-- Libya section -->
      <% include _partials/_libya %>

      <!-- Nigeria section -->
      <% include _partials/_nigeria %>

      <!-- South Sudan section -->
      <% include _partials/_southsudan %>

      <!-- Syria section -->
      <% include _partials/_syria %>

      <!-- Yemen section -->
	  <% include _partials/_yemen %>
	  
	  <!-- Preparedness section -->
	  <% include _partials/_preparedness %>
	  
	  <!-- Strategy section -->
      <% include _partials/_strategy %>

      <!-- Thank you section -->
      <% include _partials/_thankyou %>
    </main>
<% include _partials/_footer %>
